"0","log2cpm.quant <- do.call(rbind, mclapply(1:nrow(log2cpm), function(g) {
  yy <- log2cpm[g,]
  is.zero <- which(yy == 0)
  qq.map <- qqnorm(yy)
  yy.qq <- qq.map$x
  yy.qq[is.zero] <- sample(qq.map$x[is.zero])
  return(yy.qq)
}, mc.cores=4) )
"
"2","
"
